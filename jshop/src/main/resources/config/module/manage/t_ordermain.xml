<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="manage.ordermain">	<select id="manage.ordermain.selectPageList" parameterType="net.jeeshop.services.manage.ordermain.bean.Ordermain" resultType="net.jeeshop.services.manage.ordermain.bean.Ordermain">	 select * 	 from t_ordermain t where 1=1  order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="manage.ordermain.selectPageCount" parameterType="net.jeeshop.services.manage.ordermain.bean.Ordermain" resultType="java.lang.Integer">	 select count(*) from (select 1 	 from t_ordermain t where 1=1 	 ) a 	</select>		<select id="manage.ordermain.selectList" parameterType="net.jeeshop.services.manage.ordermain.bean.Ordermain" resultType="net.jeeshop.services.manage.ordermain.bean.Ordermain">	 <!-- select * 	 from t_ordermain t where 1=1 	 <if test="orderID!=0"> and orderID=#{orderID} </if> order by id  --> select * 	 from t_ordermain t where 1=1 	 <if test="orderID!=0"> and orderID=#{orderID} </if>	 <if test="queryOrderIDs!=null and queryOrderIDs.size &gt; 0">	  and orderID in <foreach item="item" index="index" collection="queryOrderIDs" open="(" separator="," close=")">    			#{item}   		</foreach> 	</if> order by mainCatalogCode,id 	</select> 	<insert id="manage.ordermain.insert" parameterType="net.jeeshop.services.manage.ordermain.bean.Ordermain" useGeneratedKeys="true" keyProperty="id">		insert into t_ordermain		(		id,createdate,orderID,number,count		<if test="subTotal!=null and subTotal!=''">		,subTotal		</if>		<if test="mainCatalogName!=null and mainCatalogName!=''">		,mainCatalogName		</if>		<if test="mainCatalogCode!=null and mainCatalogCode!=''">		,mainCatalogCode		</if>		<if test="remark!=null and remark!=''">		,remark		</if>		)		values		(		id,now(),#{orderID},#{number},#{count}		<if test="subTotal!=null and subTotal!=''">		,#{subTotal}		</if>		<if test="mainCatalogName!=null and mainCatalogName!=''">		,#{mainCatalogName}		</if>		<if test="mainCatalogCode!=null and mainCatalogCode!=''">		,#{mainCatalogCode}		</if>		<if test="remark!=null and remark!=''">		,#{remark}		</if>	)</insert>		<select id="manage.ordermain.selectOne" parameterType="net.jeeshop.services.manage.ordermain.bean.Ordermain" resultType="net.jeeshop.services.manage.ordermain.bean.Ordermain">	 select * 	 from t_ordermain t where 1=1 and id=#{id}	</select>		<select id="manage.ordermain.selectCount" parameterType="net.jeeshop.services.manage.ordermain.bean.Ordermain" resultType="int">	 select count(*) from t_ordermain t where 1=1 	<if test="orderID!=0"> and orderID=#{orderID} </if>	</select>	</mapper>