<?xml version="1.0" encoding="GBK"?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- create by autoCode,v1.0.0 --><mapper namespace="manage.commision">	<select id="manage.commision.selectPageList" parameterType="net.jeeshop.services.manage.commision.bean.Commision" resultType="net.jeeshop.services.manage.commision.bean.Commision">	 select t.id,t.commisionStatus,t.commisionTime,t.commisionCheckTime,t.commisionCheckUser,t.account,t.serialId,t.ptotal,t.createdate,t.commisionUser,t.commisionSale,t.commisionTime,t.commision,(select username from t_user r where t.commisionUser=r.usercode) username from t_order t where 1=1 	 <if test="serialId!=null and serialId!=''">	  and serialId=#{serialId}	 </if>	 <if test="account!=null and account!=''">		and t.account=#{account}	 </if>	 <if test="commisionStatus!=null and commisionStatus!=''">		and t.commisionStatus=#{commisionStatus}	 </if>	 <if test="startDate!=null and startDate!=''">		and t.createdate &gt;= #{startDate}	 </if>	 <if test="endDate!=null and endDate!=''">		and t.createdate &lt;= #{endDate}	 </if>	  <if test="username!=null and username!=''">		 and username=#{username}	</if>	<if test="commisionCheckUser!=null and commisionCheckUser!=''">commisionUser		 and commisionCheckUser=#{commisionCheckUser}	</if>	<if test="commisionUser!=null and commisionUser!=''">		 and commisionUser=#{commisionUser}	</if>	 <!-- 数据范围过滤 -->	${sqlMap.dsf}	 order by id desc  limit #{offset},#{pageSize} 	</select> 	<select id="manage.commision.selectPageCount" parameterType="net.jeeshop.services.manage.commision.bean.Commision" resultType="java.lang.Integer">	 select count(1) from t_order t where 1=1 	<if test="serialId!=null and serialId!=''">	  and serialId=#{serialId}	 </if>	 <if test="account!=null and account!=''">		and t.account=#{account}	 </if>	 <if test="commisionStatus!=null and commisionStatus!=''">		and t.commisionStatus=#{commisionStatus}	 </if>	 <if test="startDate!=null and startDate!=''">		and t.createdate &gt;= #{startDate}	 </if>	 <if test="endDate!=null and endDate!=''">		and t.createdate &lt;= #{endDate}	 </if>	  <if test="username!=null and username!=''">		 and username=#{username}	</if>	<if test="commisionCheckUser!=null and commisionCheckUser!=''">		 and commisionCheckUser=#{commisionCheckUser}	</if>	<if test="commisionUser!=null and commisionUser!=''">		 and commisionUser=#{commisionUser}	</if>	 <!-- 数据范围过滤 -->	${sqlMap.dsf}		</select>		 	<update id="manage.commision.update" parameterType="net.jeeshop.services.manage.commision.bean.Commision">		update t_order		  set commisionStatus=#{commisionStatus}		<if test="commisionUser!=null and commisionUser!=''">		,commisionUser=#{commisionUser}		</if>		<if test="commisionTime!=null and commisionTime!=''">		,commisionTime=#{commisionTime}		</if>		<if test="commisionCheckTime!=null and commisionCheckTime!=''">		,commisionCheckTime=#{commisionCheckTime}		</if>		<if test="commisionCheckUser!=null and commisionCheckUser!=''">		,commisionCheckUser=#{commisionCheckUser}		</if>				<if test="commisionSale !=null and commisionSale !=''">		,commisionSale=#{commisionSale}		</if>		<if test="commision !=null and commision !=''">		,commision=#{commision}		</if>		<if test="commisionTime !=null and commisionTime !=''">		,commisionTime=#{commisionTime}		</if>		 where 1=1 and id=#{id}	</update></mapper>